<% provide(:title, (I18n.t :list, scope: [:page_title, :auction_item])) %>
<div class="well">
  <%= render "search_form" %>
</div>

<table class="table table-striped table-bordered table-hover table-responsive">
  <tr class="success">
    <th><%= AuctionItem.h(:item_name) %></th>
    <th><%= AuctionItem.h(:auction_date) %></th>
    <th></th>
    <th></th>
  </tr>
  <% @newss.each do |news| %>
    <tr>
      <td><%= link_to news.title, admin_auction_item_path(news) %></td>
      <td><%= news.public_from.time_to_date %></td>
      <td><%= link_to I18n.t(:edit), edit_admin_auction_item_path(news) %> </td>
      <td>
        <%= link_to I18n.t(:destroy),
          admin_auction_item_path(news),
          method: :delete,
          data: {confirm: I18n.t(:confirmation)} %>
      </td>
    </tr>
  <% end %>
</table>
